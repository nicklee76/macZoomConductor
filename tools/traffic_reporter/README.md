# browserNexus Daily GA4 Traffic Reporter

Pulls daily traffic statistics (active users, sessions, pageviews, top sources, custom events) from Google Analytics 4 API and sends a formatted Markdown report to Telegram.
